# pre-config hyper-params for each dataset

pre_config = {
    "iris": dict(
        n_samples=100,
        n_components=2,
        batch_size=0,
        epochs=100,
        learning_rate=130,
        random_state=42,
    ),
    "digits": dict(
        n_samples=100,
        n_components=2,
        batch_size=0,
        epochs=100,
        learning_rate=130,
        random_state=42,
    ),
    "wine": dict(
        n_samples=100,
        n_components=2,
        batch_size=0,
        epochs=100,
        learning_rate=130,
        random_state=42,
    ),
    "breast_cancer": dict(
        n_samples=100,
        n_components=2,
        batch_size=0,
        epochs=100,
        learning_rate=130,
        random_state=42,
    ),
}
